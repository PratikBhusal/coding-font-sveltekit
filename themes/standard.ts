import type { CustomThemeConfig } from '@skeletonlabs/tw-plugin';
import { standardThemeProperties } from '../src/lib/standardThemeProperties';

export const standardTheme: CustomThemeConfig = {
  name: 'standard',
  properties: standardThemeProperties
};
